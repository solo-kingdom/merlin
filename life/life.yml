info:
  product: merlin
  version: 0.0.1
modules:
  global:
    type: sample
    build:
    package:
      base: .
      exclude:
        - requirements
      rewrite:
        - from: install.sh
          to: install
        - from: requirements
          to: require
    deploy:
      package: global
