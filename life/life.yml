info:
  space: merlin
  product: merlin
  version: 0.0.1
modules:
  global:
    type: sample
    build:
    package:
      # TODO base 默认为与模块同名文件夹
      base: .
      excludes:
        - requirements
  install:
    type: sample
    build:
    package:
      base: .
      includes:
        - install.sh
