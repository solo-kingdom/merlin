info:
  space: merlin
  product: merlin
  version: 0.0.1
modules:
  merlin:
    type: sample
    build:
    package:
      # TODO base 默认为与模块同名文件夹
      base: .
      excludes:
        - requirements
        - '*.git'
        - '*.gitignore'
  install:
    type: no-pack
    build:
    package:
      base: .
      includes:
        - install.sh
